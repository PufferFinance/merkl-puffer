import type { ReactNode } from "react";

export type TooltipLayoutProps = {
  header: ReactNode;
  title: ReactNode;
  content: ReactNode;
};

export default function TooltipLayout() {
  return <></>;
}
